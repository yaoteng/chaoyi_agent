---
name: my-skill
description: 一句话说明这个 Skill 解决什么场景、何时触发。必须写清「当……时候用」，这是宿主判断要不要加载它的唯一依据。例如：当用户想为开源社区自动化处理某类重复运营动作时使用。
version: 0.1.0
license: MIT
compatible_hosts: [Claude Code, WorkBuddy, Cursor, Codex]
standards: [Skills 1.0, MCP 0.4]
---

# <Skill 中文名>

## When to use（何时用）
- 触发场景 1
- 触发场景 2

## Steps（执行步骤）
1. **理解意图**：从用户请求中提取关键参数（如仓库、时间范围、语言对）。
2. **收集信息**：通过 MCP 工具或宿主能力读取所需数据（议题、commit、贡献记录等）。
3. **处理**：按下方规则执行核心逻辑。
4. **产出草稿**：生成可供人确认的结果，不要直接执行破坏性动作。
5. **请求确认**：对外动作（发言 / 合并 / 派发）需用户确认后再落地。

## Output（输出格式）
给出清晰、可复制粘贴的结果，例如 Markdown 表格、PR-ready diff、待发送话术。

## Guardrails（护栏 / 红线）
- 涉及对外发言、合并、封禁等，只出草稿，必须 HITL。
- 不得绕过社区 CoC / 平台规则。
- 不得伪造或篡改任何数据。
- 隐私数据仅用于本次任务，不外泄。

## Notes（备注）
- 可依赖的 MCP 工具 / 宿主能力。
- 已知边界与降级方案。
