# 决策确认与执行记录

> 时间：2026-09-03
> 背景：项目从「潮驿直播（直播社交产品）」转向「潮驿 Agent（社区 agent 化工具箱）」。以下 5 项为用户拍板结果，并已落地执行。

## 一、5 项关键决策（用户原话）

| # | 议题 | 决策 |
|---|---|---|
| 1 | 先做 Skills 库，还是 Agents 平台？ | **Skills 库**（最轻、最易起量） |
| 2 | 目标社群是否以开源社区为绝对切入点？ | **是** |
| 3 | 现有直播代码：保留为 demo，还是彻底归档？ | **彻底归档**（不保留为 demo） |
| 4 | 是否明确定位「兼容 Skills + MCP，跨宿主运行」？ | **定位明确** |
| 5 | 文件夹 `chaoyi_live/` 是否改名 `chaoyi_agent/`？ | **改为 `chaoyi_agent/`** |

## 二、执行落点

### 1. Skills 库首发（决策 1、2、4）
在 `chaoyi_agent/skills/` 下落地：
- `_template/`：新技能脚手架（SKILL.md 标准字段 + 四段式结构）。
- `community-onboarding/`：贡献者入驻。
- `issue-triage/`：议题管家。
- `release-notes/`：发布说明。
- `doc-localization/`：文档本地化。
- `contributor-recognition/`：贡献致谢。
- `skills/README.md`：技能索引 + 接入说明 + 合规红线。

全部遵循 **Skills 1.0** 规范、标注 **MCP 0.4** 兼容、声明跨宿主（Claude Code / WorkBuddy / Cursor / Codex）、MIT 许可。

### 2. 开源社区为绝对切入点（决策 2）
- 重新定位方案、竞品分析均已锁定「开源 / 开发者 / 兴趣社群」为唯一目标客群。
- 5 个首发 Skill 均围绕社群运营痛点设计（非通用 AGI）。

### 3. 直播代码彻底归档（决策 3）
- `chaoyi_live/` 内 9 份直播方向文档整体移入 `chaoyi_agent/archive/`（竞品报告、PRD、合规清单、牌照路线图、开源化战略、合并方案、可二次开发分析、千星战术书直播版、竞品速览）。
- 原 `chaoyi-kitchen/` 仓库（四端 WebRTC 直播 monorepo）作为**历史参考实现归档**，不在新项目中作为 demo 保留。
- 新项目 `chaoyi_agent/` 为纯文档 + Skills 库，不含直播运行代码。

### 4. 定位明确（决策 4）
- 定位一句话：「面向开源社区的 Agent/Skill 工具箱 + 改造方法论，让小社群拥有数字社工」。
- 技术定位：兼容 **Skills 1.0 + MCP 0.4**，跨宿主运行。已写入 README、skills/README、各 SKILL.md frontmatter。

### 5. 文件夹改名（决策 5）
- `chaoyi_live/` → `chaoyi_agent/`（已执行 `mv`）。
- 新结构：`README.md` / `LICENSE` / `skills/` / 两份战略文档 / `决策确认与执行记录.md` / `archive/`。

## 三、与前期文档的关系
- 保留并继续有效：`潮驿项目重新定位方案.md`、`潮驿Agent项目竞品分析.md`。
- 新增：`潮驿Agent一周千星战术战略书.md`（复用已归档直播战术书的渠道/节奏，替换 hook 与素材）。
- 归档（仅供参考，非当前方向）：`archive/` 下全部直播文档。

## 四、下一步建议
1. 将 `chaoyi_agent/` 作为独立 GitHub repo 初始化并发布 `v0.1.0`（含 5 Skills + 模板）。
2. 按《潮驿Agent一周千星战术战略书》执行 D-1 准备与七日作战。
3. 视社区反馈，规划 MCP 网关与 Agents 多角色模板（决策 1 中「平台」作为后续阶段）。

## 五、2026-09-05 分销业务定位转向（用户拍板）

**背景**：仓库已发布、冲星战役待启动，但用户复核后指出——原 5 个 Skill（community-onboarding / issue-triage / release-notes / doc-localization / contributor-recognition）均为**开源开发者社区运营**技能，与用户主营的**分销 / 快消 / 经销商多渠道触达业务（disttouch / WorkBuddy / FMCG 分销 SaaS）** 不相干。用户明确选择「**围绕分销重做**」。

**执行落点（已全部落地）**：
- 5 个 Skill 重写为分销业务域：
  - `distributor-onboarding`（经销商入驻引导，原 community-onboarding）
  - `reach-campaign`（多渠道触达编排，原 contributor-recognition）
  - `channel-inspection`（渠道健康巡检，原 issue-triage）
  - `order-inventory-reconcile`（订单库存对账，原 release-notes）
  - `visit-plan`（拜访计划编排，原 doc-localization）
- 4 个 Agent 角色重写为分销班组：入驻向导 / 触达管家 / 渠道巡检员 / 拜访统筹。
- `examples/reference-distribution`：一家 1,200 家下游门店的快消分销商 agent 化 before/after 样例（月度工时 40h→6h）。
- 根 README、skills/README、mcp-server/README、架构图、路线图、决策记录均翻转为分销定位。
- MCP 网关 `server.py` 与 `validate_skills.py` 为域无关脚手架，无需改动，自动加载新 Skill。

**战略含义**：chaoyi_agent 不再是独立开源星标项目，而成为**用户分销 SaaS 能力的 agent 化外衣**——宣传时可名正言顺写「我把跑着的分销业务 agent 化了」。冲星受众也从开发者社群切换到分销 / 快消 / 销售运营从业者。
